rule_id: dart-network-001
language: dart
examples:
  unsafe: '// Unsafe - dart-network-001

    var rng = Random();

    '
  safe: '// Safe - dart-network-001

    // Use parameterized APIs, validate inputs

    final stmt = await db.prepare(''SELECT * FROM t WHERE id = ?'');

    '
