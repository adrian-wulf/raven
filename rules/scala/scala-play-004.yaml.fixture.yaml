rule_id: scala-play-004
language: scala
examples:
  unsafe: '// Unsafe - scala-play-004

    sql"SELECT * FROM users WHERE id = $userId"

    '
  safe: '// Safe - scala-play-004

    // Use ORM, parameterized queries

    Users.filter(_.id === userId).result

    '
