rule_id: docker-mount-001
language: dockerfile
examples:
  unsafe: '# Unsafe - docker-mount-001

    # No HEALTHCHECK

    '
  safe: '# Safe - docker-mount-001

    # Use pinned versions, non-root user, secrets mount

    FROM ubuntu:22.04

    USER appuser

    '
