rule_id: ex-crypto-001
language: elixir
examples:
  unsafe: '# Unsafe - ex-crypto-001

    Repo.query("SELECT * FROM users WHERE id = #{user_id}")

    '
  safe: '# Safe - ex-crypto-001

    # Use Ecto changesets, parameterized queries

    from u in User, where: u.id == ^user_id

    '
