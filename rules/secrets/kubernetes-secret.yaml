id: raven-secrets-k8s-001
name: Kubernetes Secret in Code
severity: critical
category: secrets
confidence: high
languages: [javascript, typescript, python, go, php, java, csharp, rust, ruby, swift, kotlin]
message: |
  Kubernetes secret value in code! Use sealed secrets or external secret operators.
patterns:
  - type: regex
    pattern: "apiVersion:\\s*v1\\s*\\nkind:\\s*Secret"
