rule_id: scala-crypto-001
language: scala
examples:
  unsafe: '// Unsafe - scala-crypto-001

    Cipher.getInstance("AES/ECB/PKCS5Padding")

    '
  safe: '// Safe - scala-crypto-001

    // Use ORM, parameterized queries

    Users.filter(_.id === userId).result

    '
