rule_id: py-fastapi-rate-002
language: python
examples:
  unsafe: '# Unsafe - py-fastapi-rate-002

    DEBUG = True

    '
  safe: '# Safe - py-fastapi-rate-002

    # Use parameterized queries, validate inputs, use security libraries

    from django.utils.html import escape

    escape(user_input)

    '
