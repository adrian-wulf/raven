id: raven-secrets-docker-001
name: Docker Registry Auth
severity: high
category: secrets
confidence: high
languages: [javascript, typescript, python, go, php, java, csharp, rust, ruby, swift, kotlin]
message: |
  Docker registry credentials detected! Use docker credential helpers or secret manager.
patterns:
  - type: regex
    pattern: "auths\\s*:\\s*\\{[^}]*auth\\s*:\\s*['\"][^'\"]+['\"]"
