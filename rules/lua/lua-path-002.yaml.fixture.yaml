rule_id: lua-path-002
language: lua
examples:
  unsafe: '-- Unsafe - lua-path-002

    io.open(user_path, "r")

    '
  safe: '-- Safe - lua-path-002

    -- Validate inputs, use parameterized APIs

    local stmt = db:prepare(''SELECT * FROM t WHERE id = ?'')

    '
