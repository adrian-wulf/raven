rule_id: yaml-k8s-015
language: yaml
examples:
  unsafe: '# Unsafe - yaml-k8s-015

    # No NetworkPolicy

    '
  safe: "# Safe - yaml-k8s-015\n# Use sealed secrets, security contexts\napiVersion: v1\nkind: Secret\nstringData:\n  password: <from-vault>\n"
