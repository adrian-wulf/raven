rule_id: docker-root-002
language: dockerfile
examples:
  unsafe: '# Unsafe - docker-root-002

    USER root

    '
  safe: '# Safe - docker-root-002

    # Use pinned versions, non-root user, secrets mount

    FROM ubuntu:22.04

    USER appuser

    '
