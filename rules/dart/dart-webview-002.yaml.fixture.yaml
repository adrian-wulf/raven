rule_id: dart-webview-002
language: dart
examples:
  unsafe: '// Unsafe - dart-webview-002

    WebView(initialUrl: userUrl, javascriptMode: JavascriptMode.unrestricted)

    '
  safe: '// Safe - dart-webview-002

    // Use parameterized APIs, validate inputs

    final stmt = await db.prepare(''SELECT * FROM t WHERE id = ?'');

    '
