rule_id: lua-path-001
language: lua
examples:
  unsafe: '-- Unsafe - lua-path-001

    io.open(user_path, "r")

    '
  safe: '-- Safe - lua-path-001

    -- Validate inputs, use parameterized APIs

    local stmt = db:prepare(''SELECT * FROM t WHERE id = ?'')

    '
