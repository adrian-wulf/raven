id: raven-iac-k8s-005
name: Kubernetes Secret in Environment Variable
severity: high
category: secrets
confidence: high
cwe: CWE-798
languages: [yaml]
message: |
  Secrets in env vars can be exposed in logs and process lists. Use volume mounts.
patterns:
  - type: regex
    pattern: "valueFrom:\\s*\\n\\s*secretKeyRef:"
