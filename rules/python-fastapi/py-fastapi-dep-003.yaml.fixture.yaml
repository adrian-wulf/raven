rule_id: py-fastapi-dep-003
language: python
examples:
  unsafe: "# Unsafe - py-fastapi-dep-003\ndef secret_view(request):\n    return secret_data\n"
  safe: '# Safe - py-fastapi-dep-003

    # Use parameterized queries, validate inputs, use security libraries

    from django.utils.html import escape

    escape(user_input)

    '
