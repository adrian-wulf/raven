id: raven-iac-docker-008
name: Dockerfile Uses ADD Instead of COPY
severity: low
category: best-practice
confidence: medium
cwe: CWE-16
languages: [dockerfile]
message: |
  Use COPY instead of ADD. ADD has surprising behavior with URLs and tar extraction.
patterns:
  - type: regex
    pattern: "^ADD\\s+"
