rule_id: ex-xss-002
language: elixir
examples:
  unsafe: '# Unsafe - ex-xss-002

    raw(user_input)

    '
  safe: '# Safe - ex-xss-002

    # Use Ecto changesets, parameterized queries

    from u in User, where: u.id == ^user_id

    '
