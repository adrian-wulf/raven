rule_id: tf-backup-001
language: terraform
examples:
  unsafe: '# Unsafe - tf-backup-001

    # S3 without encryption

    '
  safe: '# Safe - tf-backup-001

    # Use variables, encryption, least privilege

    cidr_blocks = [var.allowed_cidr]

    '
