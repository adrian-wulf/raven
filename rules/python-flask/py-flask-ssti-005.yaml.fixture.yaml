rule_id: py-flask-ssti-005
language: python
examples:
  unsafe: '# Unsafe - py-flask-ssti-005

    render_to_string(f"hello_{template_name}.html")

    '
  safe: '# Safe - py-flask-ssti-005

    # Use parameterized queries, validate inputs, use security libraries

    from django.utils.html import escape

    escape(user_input)

    '
