rule_id: scala-template-001
language: scala
examples:
  unsafe: '// Unsafe - scala-template-001

    sql"SELECT * FROM users WHERE id = $userId"

    '
  safe: '// Safe - scala-template-001

    // Use ORM, parameterized queries

    Users.filter(_.id === userId).result

    '
