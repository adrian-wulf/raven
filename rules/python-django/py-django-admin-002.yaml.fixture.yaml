rule_id: py-django-admin-002
language: python
examples:
  unsafe: "# Unsafe - py-django-admin-002\ndef secret_view(request):\n    return secret_data\n"
  safe: '# Safe - py-django-admin-002

    # Use parameterized queries, validate inputs, use security libraries

    from django.utils.html import escape

    escape(user_input)

    '
