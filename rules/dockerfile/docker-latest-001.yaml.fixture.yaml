rule_id: docker-latest-001
language: dockerfile
examples:
  unsafe: '# Unsafe - docker-latest-001

    FROM ubuntu:latest

    '
  safe: '# Safe - docker-latest-001

    # Use pinned versions, non-root user, secrets mount

    FROM ubuntu:22.04

    USER appuser

    '
