rule_id: docker-cmd-001
language: dockerfile
examples:
  unsafe: '# Unsafe - docker-cmd-001

    FROM ubuntu:latest

    '
  safe: '# Safe - docker-cmd-001

    # Use pinned versions, non-root user, secrets mount

    FROM ubuntu:22.04

    USER appuser

    '
