rule_id: py-flask-cookie-002
language: python
examples:
  unsafe: '# Unsafe - py-flask-cookie-002

    import hashlib

    hashlib.md5(password.encode())

    '
  safe: '# Safe - py-flask-cookie-002

    # Use parameterized queries, validate inputs, use security libraries

    from django.utils.html import escape

    escape(user_input)

    '
