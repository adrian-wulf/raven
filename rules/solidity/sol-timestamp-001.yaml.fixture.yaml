rule_id: sol-timestamp-001
language: solidity
examples:
  unsafe: '// Unsafe - sol-timestamp-001

    if (block.timestamp > deadline) { }

    '
  safe: '// Safe - sol-timestamp-001

    // Use Checks-Effects-Interactions, access controls

    // ReentrancyGuard, SafeMath

    '
