rule_id: docker-mount-002
language: dockerfile
examples:
  unsafe: '# Unsafe - docker-mount-002

    USER root

    '
  safe: '# Safe - docker-mount-002

    # Use pinned versions, non-root user, secrets mount

    FROM ubuntu:22.04

    USER appuser

    '
