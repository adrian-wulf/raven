id: raven-iac-gh-003
name: GitHub Actions Secret in Environment
severity: high
category: secrets
confidence: high
languages: [yaml]
message: |
  Secret exposed in env var. Use secrets context directly in steps instead.
patterns:
  - type: regex
    pattern: "env:\\s*\\n\\s*\\w+:\\s*\\$\\{\\{\\s*secrets\\."
