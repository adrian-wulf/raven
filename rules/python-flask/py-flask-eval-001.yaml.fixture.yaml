rule_id: py-flask-eval-001
language: python
examples:
  unsafe: '# Unsafe - py-flask-eval-001

    eval(user_code)

    '
  safe: '# Safe - py-flask-eval-001

    # Use parameterized queries, validate inputs, use security libraries

    from django.utils.html import escape

    escape(user_input)

    '
