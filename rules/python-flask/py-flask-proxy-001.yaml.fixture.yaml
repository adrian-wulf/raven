rule_id: py-flask-proxy-001
language: python
examples:
  unsafe: '# Unsafe - py-flask-proxy-001

    DEBUG = True

    '
  safe: '# Safe - py-flask-proxy-001

    # Use parameterized queries, validate inputs, use security libraries

    from django.utils.html import escape

    escape(user_input)

    '
