rule_id: ex-eval-003
language: elixir
examples:
  unsafe: '# Unsafe - ex-eval-003

    Code.eval_string(user_code)

    '
  safe: '# Safe - ex-eval-003

    # Use Ecto changesets, parameterized queries

    from u in User, where: u.id == ^user_id

    '
