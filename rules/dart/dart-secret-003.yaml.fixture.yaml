rule_id: dart-secret-003
language: dart
examples:
  unsafe: '// Unsafe - dart-secret-003

    const apiKey = ''sk-live-abc123'';

    '
  safe: '// Safe - dart-secret-003

    // Use parameterized APIs, validate inputs

    final stmt = await db.prepare(''SELECT * FROM t WHERE id = ?'');

    '
