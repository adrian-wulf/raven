rule_id: dart-ssrf-001
language: dart
examples:
  unsafe: '// Unsafe - dart-ssrf-001

    http.get(userUrl);

    '
  safe: '// Safe - dart-ssrf-001

    // Use parameterized APIs, validate inputs

    final stmt = await db.prepare(''SELECT * FROM t WHERE id = ?'');

    '
