rule_id: js-angular-auth-001
language: javascript
examples:
  unsafe: '// Unsafe - js-angular-auth-001

    router.push(''/admin'');

    '
  safe: '// Safe - js-angular-auth-001

    // Use framework features, validate inputs, sanitize data

    const safeHtml = DOMPurify.sanitize(userData);

    '
