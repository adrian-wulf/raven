rule_id: sol-delegatecall-002
language: solidity
examples:
  unsafe: '// Unsafe - sol-delegatecall-002

    address(delegate).delegatecall(data);

    '
  safe: '// Safe - sol-delegatecall-002

    // Use Checks-Effects-Interactions, access controls

    // ReentrancyGuard, SafeMath

    '
