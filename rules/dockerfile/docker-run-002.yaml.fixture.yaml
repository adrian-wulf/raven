rule_id: docker-run-002
language: dockerfile
examples:
  unsafe: '# Unsafe - docker-run-002

    FROM ubuntu:latest

    '
  safe: '# Safe - docker-run-002

    # Use pinned versions, non-root user, secrets mount

    FROM ubuntu:22.04

    USER appuser

    '
