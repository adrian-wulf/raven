id: raven-secrets-jwt-001
name: Hardcoded JWT Secret
severity: critical
category: secrets
confidence: high
languages: [javascript, typescript, python, go, php, java, csharp, rust, ruby, swift, kotlin]
message: |
  Hardcoded JWT secret! Generate cryptographically random secret and store securely.
patterns:
  - type: regex
    pattern: "jwt[Ss]ecret\\s*[:=]\\s*['\"][^'\"]{8,}['\"]"
  - type: regex
    pattern: "secretOrKey\\s*[:=]\\s*['\"][^'\"]{8,}['\"]"
