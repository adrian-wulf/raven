rule_id: js-angular-crypto-003
language: javascript
examples:
  unsafe: '// Unsafe - js-angular-crypto-003

    const token = Math.random();

    '
  safe: '// Safe - js-angular-crypto-003

    // Use framework features, validate inputs, sanitize data

    const safeHtml = DOMPurify.sanitize(userData);

    '
