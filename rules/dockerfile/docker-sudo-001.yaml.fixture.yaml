rule_id: docker-sudo-001
language: dockerfile
examples:
  unsafe: '# Unsafe - docker-sudo-001

    USER root

    '
  safe: '# Safe - docker-sudo-001

    # Use pinned versions, non-root user, secrets mount

    FROM ubuntu:22.04

    USER appuser

    '
