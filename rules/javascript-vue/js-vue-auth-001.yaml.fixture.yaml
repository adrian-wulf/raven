rule_id: js-vue-auth-001
language: javascript
examples:
  unsafe: '// Unsafe - js-vue-auth-001

    router.push(''/admin'');

    '
  safe: '// Safe - js-vue-auth-001

    // Use framework features, validate inputs, sanitize data

    const safeHtml = DOMPurify.sanitize(userData);

    '
