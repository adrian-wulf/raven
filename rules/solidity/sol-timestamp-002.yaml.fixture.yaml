rule_id: sol-timestamp-002
language: solidity
examples:
  unsafe: '// Unsafe - sol-timestamp-002

    if (block.timestamp > deadline) { }

    '
  safe: '// Safe - sol-timestamp-002

    // Use Checks-Effects-Interactions, access controls

    // ReentrancyGuard, SafeMath

    '
