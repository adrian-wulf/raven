rule_id: docker-root-001
language: dockerfile
examples:
  unsafe: '# Unsafe - docker-root-001

    USER root

    '
  safe: '# Safe - docker-root-001

    # Use pinned versions, non-root user, secrets mount

    FROM ubuntu:22.04

    USER appuser

    '
