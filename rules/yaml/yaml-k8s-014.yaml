id: yaml-k8s-014
name: Missing livenessProbe
severity: low
category: config
confidence: low
cwe: CWE-400
languages:
- yaml
message: Container without livenessProbe is not health-checked. Add liveness probe.
patterns:
- type: regex
  pattern: containers:\s*\n.*image:\s*\S+(?!.*livenessProbe)
  where: []
references:
- https://cwe.mitre.org/data/definitions/400.html
