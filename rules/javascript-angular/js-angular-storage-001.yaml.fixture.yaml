rule_id: js-angular-storage-001
language: javascript
examples:
  unsafe: '// Unsafe - js-angular-storage-001

    const token = Math.random();

    '
  safe: '// Safe - js-angular-storage-001

    // Use framework features, validate inputs, sanitize data

    const safeHtml = DOMPurify.sanitize(userData);

    '
