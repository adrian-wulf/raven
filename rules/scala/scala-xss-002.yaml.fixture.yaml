rule_id: scala-xss-002
language: scala
examples:
  unsafe: '// Unsafe - scala-xss-002

    Html(userHtml)

    '
  safe: '// Safe - scala-xss-002

    // Use ORM, parameterized queries

    Users.filter(_.id === userId).result

    '
