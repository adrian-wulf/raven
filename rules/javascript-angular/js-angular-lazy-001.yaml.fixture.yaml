rule_id: js-angular-lazy-001
language: javascript
examples:
  unsafe: '// Unsafe - js-angular-lazy-001

    router.push(''/admin'');

    '
  safe: '// Safe - js-angular-lazy-001

    // Use framework features, validate inputs, sanitize data

    const safeHtml = DOMPurify.sanitize(userData);

    '
