rule_id: js-angular-eval-002
language: javascript
examples:
  unsafe: '// Unsafe - js-angular-eval-002

    eval(userCode);

    '
  safe: '// Safe - js-angular-eval-002

    // Use framework features, validate inputs, sanitize data

    const safeHtml = DOMPurify.sanitize(userData);

    '
