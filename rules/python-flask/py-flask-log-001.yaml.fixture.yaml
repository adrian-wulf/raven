rule_id: py-flask-log-001
language: python
examples:
  unsafe: '# Unsafe - py-flask-log-001

    logger.info(f"User login: {username}")

    '
  safe: '# Safe - py-flask-log-001

    # Use parameterized queries, validate inputs, use security libraries

    from django.utils.html import escape

    escape(user_input)

    '
