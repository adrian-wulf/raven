rule_id: py-flask-cache-001
language: python
examples:
  unsafe: '# Unsafe - py-flask-cache-001

    import pickle

    pickle.loads(user_data)

    '
  safe: '# Safe - py-flask-cache-001

    # Use parameterized queries, validate inputs, use security libraries

    from django.utils.html import escape

    escape(user_input)

    '
