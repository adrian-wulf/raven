rule_id: tf-sg-002
language: terraform
examples:
  unsafe: '# Unsafe - tf-sg-002

    cidr_blocks = ["0.0.0.0/0"]

    '
  safe: '# Safe - tf-sg-002

    # Use variables, encryption, least privilege

    cidr_blocks = [var.allowed_cidr]

    '
