rule_id: sol-multisig-001
language: solidity
examples:
  unsafe: "// Unsafe - sol-multisig-001\nfunction withdraw() public {\n    msg.sender.call{value: balance}(\"\");\n}\n"
  safe: '// Safe - sol-multisig-001

    // Use Checks-Effects-Interactions, access controls

    // ReentrancyGuard, SafeMath

    '
