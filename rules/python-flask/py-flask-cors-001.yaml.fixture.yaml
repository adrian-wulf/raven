rule_id: py-flask-cors-001
language: python
examples:
  unsafe: '# Unsafe - py-flask-cors-001

    CORS(app, origins=''*'')

    '
  safe: '# Safe - py-flask-cors-001

    # Use parameterized queries, validate inputs, use security libraries

    from django.utils.html import escape

    escape(user_input)

    '
