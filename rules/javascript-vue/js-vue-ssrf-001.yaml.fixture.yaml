rule_id: js-vue-ssrf-001
language: javascript
examples:
  unsafe: '// Unsafe - js-vue-ssrf-001

    axios.get(userUrl);

    '
  safe: '// Safe - js-vue-ssrf-001

    // Use framework features, validate inputs, sanitize data

    const safeHtml = DOMPurify.sanitize(userData);

    '
