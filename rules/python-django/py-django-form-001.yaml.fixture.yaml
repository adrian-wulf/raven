rule_id: py-django-form-001
language: python
examples:
  unsafe: "# Unsafe - py-django-form-001\ndef process(data):\n    return data['value']\n"
  safe: '# Safe - py-django-form-001

    # Use parameterized queries, validate inputs, use security libraries

    from django.utils.html import escape

    escape(user_input)

    '
