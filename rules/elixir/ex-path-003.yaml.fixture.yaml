rule_id: ex-path-003
language: elixir
examples:
  unsafe: '# Unsafe - ex-path-003

    File.read!(user_path)

    '
  safe: '# Safe - ex-path-003

    # Use Ecto changesets, parameterized queries

    from u in User, where: u.id == ^user_id

    '
