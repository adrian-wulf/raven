rule_id: dart-crypto-001
language: dart
examples:
  unsafe: '// Unsafe - dart-crypto-001

    var rng = Random();

    '
  safe: '// Safe - dart-crypto-001

    // Use parameterized APIs, validate inputs

    final stmt = await db.prepare(''SELECT * FROM t WHERE id = ?'');

    '
