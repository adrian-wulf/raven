rule_id: py-flask-auth-003
language: python
examples:
  unsafe: '# Unsafe - py-flask-auth-003

    import hashlib

    hashlib.md5(password.encode())

    '
  safe: '# Safe - py-flask-auth-003

    # Use parameterized queries, validate inputs, use security libraries

    from django.utils.html import escape

    escape(user_input)

    '
