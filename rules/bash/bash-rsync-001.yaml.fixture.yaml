rule_id: bash-rsync-001
language: bash
examples:
  unsafe: '# Unsafe - bash-rsync-001

    cursor.execute(f"SELECT * FROM users WHERE id = {user_id}")

    '
  safe: '# Safe - bash-rsync-001

    # Use parameterized queries, validate inputs, use security libraries

    from django.utils.html import escape

    escape(user_input)

    '
