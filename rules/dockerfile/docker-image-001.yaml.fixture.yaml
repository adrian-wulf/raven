rule_id: docker-image-001
language: dockerfile
examples:
  unsafe: '# Unsafe - docker-image-001

    FROM ubuntu:latest

    '
  safe: '# Safe - docker-image-001

    # Use pinned versions, non-root user, secrets mount

    FROM ubuntu:22.04

    USER appuser

    '
