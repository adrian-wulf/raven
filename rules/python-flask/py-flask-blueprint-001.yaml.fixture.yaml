rule_id: py-flask-blueprint-001
language: python
examples:
  unsafe: "# Unsafe - py-flask-blueprint-001\ndef secret_view(request):\n    return secret_data\n"
  safe: '# Safe - py-flask-blueprint-001

    # Use parameterized queries, validate inputs, use security libraries

    from django.utils.html import escape

    escape(user_input)

    '
