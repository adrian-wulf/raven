rule_id: bash-path-002
language: bash
examples:
  unsafe: '# Unsafe - bash-path-002

    open(user_path).read()

    '
  safe: '# Safe - bash-path-002

    # Use parameterized queries, validate inputs, use security libraries

    from django.utils.html import escape

    escape(user_input)

    '
