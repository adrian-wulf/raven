rule_id: scala-logger-001
language: scala
examples:
  unsafe: '// Unsafe - scala-logger-001

    sql"SELECT * FROM users WHERE id = $userId"

    '
  safe: '// Safe - scala-logger-001

    // Use ORM, parameterized queries

    Users.filter(_.id === userId).result

    '
