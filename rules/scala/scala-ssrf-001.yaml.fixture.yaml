rule_id: scala-ssrf-001
language: scala
examples:
  unsafe: '// Unsafe - scala-ssrf-001

    WS.url(userUrl).get()

    '
  safe: '// Safe - scala-ssrf-001

    // Use ORM, parameterized queries

    Users.filter(_.id === userId).result

    '
