rule_id: bash-path-001
language: bash
examples:
  unsafe: '# Unsafe - bash-path-001

    open(user_path).read()

    '
  safe: '# Safe - bash-path-001

    # Use parameterized queries, validate inputs, use security libraries

    from django.utils.html import escape

    escape(user_input)

    '
