rule_id: js-angular-auth-003
language: javascript
examples:
  unsafe: '// Unsafe - js-angular-auth-003

    router.push(''/admin'');

    '
  safe: '// Safe - js-angular-auth-003

    // Use framework features, validate inputs, sanitize data

    const safeHtml = DOMPurify.sanitize(userData);

    '
