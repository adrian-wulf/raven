rule_id: sol-selfdestruct-001
language: solidity
examples:
  unsafe: "// Unsafe - sol-selfdestruct-001\nfunction withdraw() public {\n    msg.sender.call{value: balance}(\"\");\n}\n"
  safe: '// Safe - sol-selfdestruct-001

    // Use Checks-Effects-Interactions, access controls

    // ReentrancyGuard, SafeMath

    '
