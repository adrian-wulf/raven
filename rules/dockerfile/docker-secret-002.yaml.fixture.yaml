rule_id: docker-secret-002
language: dockerfile
examples:
  unsafe: '# Unsafe - docker-secret-002

    ENV API_KEY=sk-live-abc123

    '
  safe: '# Safe - docker-secret-002

    # Use pinned versions, non-root user, secrets mount

    FROM ubuntu:22.04

    USER appuser

    '
