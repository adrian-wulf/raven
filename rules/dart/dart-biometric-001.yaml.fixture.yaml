rule_id: dart-biometric-001
language: dart
examples:
  unsafe: '// Unsafe - dart-biometric-001

    var rng = Random();

    '
  safe: '// Safe - dart-biometric-001

    // Use parameterized APIs, validate inputs

    final stmt = await db.prepare(''SELECT * FROM t WHERE id = ?'');

    '
