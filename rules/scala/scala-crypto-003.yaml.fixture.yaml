rule_id: scala-crypto-003
language: scala
examples:
  unsafe: '// Unsafe - scala-crypto-003

    Cipher.getInstance("AES/ECB/PKCS5Padding")

    '
  safe: '// Safe - scala-crypto-003

    // Use ORM, parameterized queries

    Users.filter(_.id === userId).result

    '
