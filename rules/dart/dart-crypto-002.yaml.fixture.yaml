rule_id: dart-crypto-002
language: dart
examples:
  unsafe: '// Unsafe - dart-crypto-002

    var rng = Random();

    '
  safe: '// Safe - dart-crypto-002

    // Use parameterized APIs, validate inputs

    final stmt = await db.prepare(''SELECT * FROM t WHERE id = ?'');

    '
