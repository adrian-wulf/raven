rule_id: py-django-cache-003
language: python
examples:
  unsafe: '# Unsafe - py-django-cache-003

    DEBUG = True

    '
  safe: '# Safe - py-django-cache-003

    # Use parameterized queries, validate inputs, use security libraries

    from django.utils.html import escape

    escape(user_input)

    '
