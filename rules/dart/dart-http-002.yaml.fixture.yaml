rule_id: dart-http-002
language: dart
examples:
  unsafe: '// Unsafe - dart-http-002

    var rng = Random();

    '
  safe: '// Safe - dart-http-002

    // Use parameterized APIs, validate inputs

    final stmt = await db.prepare(''SELECT * FROM t WHERE id = ?'');

    '
