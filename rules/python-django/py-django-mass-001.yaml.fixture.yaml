rule_id: py-django-mass-001
language: python
examples:
  unsafe: "# Unsafe - py-django-mass-001\ndef __init__(self, **kwargs):\n    for k, v in kwargs.items():\n        setattr(self, k, v)\n"
  safe: '# Safe - py-django-mass-001

    # Use parameterized queries, validate inputs, use security libraries

    from django.utils.html import escape

    escape(user_input)

    '
