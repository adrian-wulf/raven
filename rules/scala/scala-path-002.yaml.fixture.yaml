rule_id: scala-path-002
language: scala
examples:
  unsafe: '// Unsafe - scala-path-002

    new File(userPath)

    '
  safe: '// Safe - scala-path-002

    // Use ORM, parameterized queries

    Users.filter(_.id === userId).result

    '
