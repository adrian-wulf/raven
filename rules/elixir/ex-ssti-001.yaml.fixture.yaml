rule_id: ex-ssti-001
language: elixir
examples:
  unsafe: '# Unsafe - ex-ssti-001

    EEx.eval_string(user_template)

    '
  safe: '# Safe - ex-ssti-001

    # Use Ecto changesets, parameterized queries

    from u in User, where: u.id == ^user_id

    '
