rule_id: docker-escape-001
language: dockerfile
examples:
  unsafe: '# Unsafe - docker-escape-001

    USER root

    '
  safe: '# Safe - docker-escape-001

    # Use pinned versions, non-root user, secrets mount

    FROM ubuntu:22.04

    USER appuser

    '
