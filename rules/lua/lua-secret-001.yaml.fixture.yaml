rule_id: lua-secret-001
language: lua
examples:
  unsafe: '-- Unsafe - lua-secret-001

    local password = "secret123"

    '
  safe: '-- Safe - lua-secret-001

    -- Validate inputs, use parameterized APIs

    local stmt = db:prepare(''SELECT * FROM t WHERE id = ?'')

    '
