id: raven-secrets-redis-001
name: Redis Password in Code
severity: high
category: secrets
confidence: high
cwe: CWE-798
languages: [javascript, typescript, python, go, php, java, csharp, rust, ruby, swift, kotlin]
message: |
  Redis password hardcoded! Use Redis ACL or secret manager.
patterns:
  - type: regex
    pattern: "(REDIS_PASSWORD|redis_password|redisPass)\\s*[:=]\\s*['\"][^'\"]+['\"]"
