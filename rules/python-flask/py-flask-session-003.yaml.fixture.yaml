rule_id: py-flask-session-003
language: python
examples:
  unsafe: '# Unsafe - py-flask-session-003

    DEBUG = True

    '
  safe: '# Safe - py-flask-session-003

    # Use parameterized queries, validate inputs, use security libraries

    from django.utils.html import escape

    escape(user_input)

    '
