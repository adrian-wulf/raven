rule_id: docker-signal-001
language: dockerfile
examples:
  unsafe: '# Unsafe - docker-signal-001

    # No HEALTHCHECK

    '
  safe: '# Safe - docker-signal-001

    # Use pinned versions, non-root user, secrets mount

    FROM ubuntu:22.04

    USER appuser

    '
