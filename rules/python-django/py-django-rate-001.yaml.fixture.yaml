rule_id: py-django-rate-001
language: python
examples:
  unsafe: '# Unsafe - py-django-rate-001

    DEBUG = True

    '
  safe: '# Safe - py-django-rate-001

    # Use parameterized queries, validate inputs, use security libraries

    from django.utils.html import escape

    escape(user_input)

    '
