rule_id: docker-apt-001
language: dockerfile
examples:
  unsafe: '# Unsafe - docker-apt-001

    FROM ubuntu:latest

    '
  safe: '# Safe - docker-apt-001

    # Use pinned versions, non-root user, secrets mount

    FROM ubuntu:22.04

    USER appuser

    '
