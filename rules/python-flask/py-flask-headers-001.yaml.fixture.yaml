rule_id: py-flask-headers-001
language: python
examples:
  unsafe: '# Unsafe - py-flask-headers-001

    DEBUG = True

    '
  safe: '# Safe - py-flask-headers-001

    # Use parameterized queries, validate inputs, use security libraries

    from django.utils.html import escape

    escape(user_input)

    '
