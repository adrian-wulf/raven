rule_id: sol-reentrancy-001
language: solidity
examples:
  unsafe: '// Unsafe - sol-reentrancy-001

    (bool success,) = msg.sender.call{value: amount}("");

    require(success, "Transfer failed");

    balances[msg.sender] -= amount;

    '
  safe: '// Safe - sol-reentrancy-001

    // Use Checks-Effects-Interactions, access controls

    // ReentrancyGuard, SafeMath

    '
