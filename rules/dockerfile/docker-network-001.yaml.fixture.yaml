rule_id: docker-network-001
language: dockerfile
examples:
  unsafe: '# Unsafe - docker-network-001

    # No HEALTHCHECK

    '
  safe: '# Safe - docker-network-001

    # Use pinned versions, non-root user, secrets mount

    FROM ubuntu:22.04

    USER appuser

    '
