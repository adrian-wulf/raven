rule_id: js-vue-http-001
language: javascript
examples:
  unsafe: '// Unsafe - js-vue-http-001

    axios.get(userUrl);

    '
  safe: '// Safe - js-vue-http-001

    // Use framework features, validate inputs, sanitize data

    const safeHtml = DOMPurify.sanitize(userData);

    '
