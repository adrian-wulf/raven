rule_id: sol-access-004
language: solidity
examples:
  unsafe: "// Unsafe - sol-access-004\nfunction withdraw() public {\n    msg.sender.call{value: balance}(\"\");\n}\n"
  safe: '// Safe - sol-access-004

    // Use Checks-Effects-Interactions, access controls

    // ReentrancyGuard, SafeMath

    '
