rule_id: js-vue-eval-001
language: javascript
examples:
  unsafe: '// Unsafe - js-vue-eval-001

    eval(userCode);

    '
  safe: '// Safe - js-vue-eval-001

    // Use framework features, validate inputs, sanitize data

    const safeHtml = DOMPurify.sanitize(userData);

    '
