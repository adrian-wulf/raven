rule_id: ex-xss-001
language: elixir
examples:
  unsafe: '# Unsafe - ex-xss-001

    raw(user_input)

    '
  safe: '# Safe - ex-xss-001

    # Use Ecto changesets, parameterized queries

    from u in User, where: u.id == ^user_id

    '
