rule_id: js-nextjs-auth-001
language: javascript
examples:
  unsafe: '// Unsafe - js-nextjs-auth-001

    router.push(''/admin'');

    '
  safe: '// Safe - js-nextjs-auth-001

    // Use framework features, validate inputs, sanitize data

    const safeHtml = DOMPurify.sanitize(userData);

    '
