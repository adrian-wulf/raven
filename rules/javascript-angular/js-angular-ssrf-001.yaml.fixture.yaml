rule_id: js-angular-ssrf-001
language: javascript
examples:
  unsafe: '// Unsafe - js-angular-ssrf-001

    axios.get(userUrl);

    '
  safe: '// Safe - js-angular-ssrf-001

    // Use framework features, validate inputs, sanitize data

    const safeHtml = DOMPurify.sanitize(userData);

    '
