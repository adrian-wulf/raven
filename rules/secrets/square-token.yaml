id: raven-secrets-square-001
name: Square API Key
severity: critical
category: secrets
confidence: high
cwe: CWE-798
languages: [javascript, typescript, python, go, php, java, csharp, rust, ruby, swift, kotlin]
message: |
  Square API key detected! Never commit payment processor keys. Use environment variables.
patterns:
  - type: regex
    pattern: "sq0idp-[0-9A-Za-z_-]{22}"
  - type: regex
    pattern: "sq0csp-[0-9A-Za-z_-]{43}"
