rule_id: py-django-config-005
language: python
examples:
  unsafe: '# Unsafe - py-django-config-005

    DEBUG = True

    '
  safe: '# Safe - py-django-config-005

    # Use parameterized queries, validate inputs, use security libraries

    from django.utils.html import escape

    escape(user_input)

    '
