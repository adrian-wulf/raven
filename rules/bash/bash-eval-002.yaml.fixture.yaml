rule_id: bash-eval-002
language: bash
examples:
  unsafe: '# Unsafe - bash-eval-002

    os.system(f"ls {directory}")

    '
  safe: '# Safe - bash-eval-002

    # Use parameterized queries, validate inputs, use security libraries

    from django.utils.html import escape

    escape(user_input)

    '
