rule_id: js-vue-vuex-001
language: javascript
examples:
  unsafe: '// Unsafe - js-vue-vuex-001

    if (env.production) { enableProdMode(); }

    '
  safe: '// Safe - js-vue-vuex-001

    // Use framework features, validate inputs, sanitize data

    const safeHtml = DOMPurify.sanitize(userData);

    '
