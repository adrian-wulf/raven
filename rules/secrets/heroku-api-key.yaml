id: raven-secrets-heroku-001
name: Heroku API Key
severity: high
category: secrets
confidence: high
languages: [javascript, typescript, python, go, php, java, csharp, rust, ruby, swift, kotlin]
message: |
  Heroku API key detected! Use Heroku config vars instead of hardcoding.
patterns:
  - type: regex
    pattern: "HEROKU_API_KEY\\s*[:=]\\s*['\"][0-9a-f]{32}['\"]"
