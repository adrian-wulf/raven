rule_id: py-fastapi-events-001
language: python
examples:
  unsafe: '# Unsafe - py-fastapi-events-001

    DEBUG = True

    '
  safe: '# Safe - py-fastapi-events-001

    # Use parameterized queries, validate inputs, use security libraries

    from django.utils.html import escape

    escape(user_input)

    '
