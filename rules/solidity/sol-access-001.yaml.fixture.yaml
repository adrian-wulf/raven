rule_id: sol-access-001
language: solidity
examples:
  unsafe: "// Unsafe - sol-access-001\nfunction withdraw() public {\n    msg.sender.call{value: balance}(\"\");\n}\n"
  safe: '// Safe - sol-access-001

    // Use Checks-Effects-Interactions, access controls

    // ReentrancyGuard, SafeMath

    '
