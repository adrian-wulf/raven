id: raven-secrets-aws-003
name: AWS Session Token
severity: critical
category: secrets
confidence: high
cwe: CWE-798
languages: [javascript, typescript, python, go, php, java, csharp, rust, ruby, swift, kotlin]
message: |
  AWS session token detected! Session tokens are temporary but still sensitive. Use IAM roles.
patterns:
  - type: regex
    pattern: "AWS_SESSION_TOKEN\\s*[:=]\\s*['\"][^'\"]+['\"]"
