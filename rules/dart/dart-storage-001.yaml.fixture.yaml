rule_id: dart-storage-001
language: dart
examples:
  unsafe: '// Unsafe - dart-storage-001

    var rng = Random();

    '
  safe: '// Safe - dart-storage-001

    // Use parameterized APIs, validate inputs

    final stmt = await db.prepare(''SELECT * FROM t WHERE id = ?'');

    '
