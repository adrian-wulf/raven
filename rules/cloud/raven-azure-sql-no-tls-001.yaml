id: raven-azure-sql-no-tls-001
name: Azure SQL without TLS enforcement
severity: high
category: cloud-security
confidence: high
cwe: CWE-16
languages: ["*"]
message: "Azure SQL server does not enforce TLS. Set minimum_tls_version."
patterns:
  - type: regex
    pattern: "minimum_tls_version.*1\\.0|minimum_tls_version.*1\\.1|tls.*disable"
