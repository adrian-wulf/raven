rule_id: dart-deser-002
language: dart
examples:
  unsafe: '// Unsafe - dart-deser-002

    jsonDecode(response.body);

    '
  safe: '// Safe - dart-deser-002

    // Use parameterized APIs, validate inputs

    final stmt = await db.prepare(''SELECT * FROM t WHERE id = ?'');

    '
