rule_id: py-flask-request-004
language: python
examples:
  unsafe: "# Unsafe - py-flask-request-004\ndef handle_upload(request):\n    file = request.FILES['file']\n    open(file.name, 'wb').write(file.read())\n"
  safe: '# Safe - py-flask-request-004

    # Use parameterized queries, validate inputs, use security libraries

    from django.utils.html import escape

    escape(user_input)

    '
