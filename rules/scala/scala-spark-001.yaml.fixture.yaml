rule_id: scala-spark-001
language: scala
examples:
  unsafe: '// Unsafe - scala-spark-001

    sql"SELECT * FROM users WHERE id = $userId"

    '
  safe: '// Safe - scala-spark-001

    // Use ORM, parameterized queries

    Users.filter(_.id === userId).result

    '
