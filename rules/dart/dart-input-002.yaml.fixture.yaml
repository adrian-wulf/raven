rule_id: dart-input-002
language: dart
examples:
  unsafe: '// Unsafe - dart-input-002

    File(userPath).readAsStringSync()

    '
  safe: '// Safe - dart-input-002

    // Use parameterized APIs, validate inputs

    final stmt = await db.prepare(''SELECT * FROM t WHERE id = ?'');

    '
