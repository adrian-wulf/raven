rule_id: js-angular-router-001
language: javascript
examples:
  unsafe: '// Unsafe - js-angular-router-001

    router.push(''/admin'');

    '
  safe: '// Safe - js-angular-router-001

    // Use framework features, validate inputs, sanitize data

    const safeHtml = DOMPurify.sanitize(userData);

    '
