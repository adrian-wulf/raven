id: raven-secrets-ssh-001
name: SSH Private Key
severity: critical
category: secrets
confidence: high
cwe: CWE-798
languages: [javascript, typescript, python, go, php, java, csharp, rust, ruby, swift, kotlin]
message: |
  SSH private key detected! This is a critical secret. Rotate immediately and use SSH agent.
patterns:
  - type: regex
    pattern: "-----BEGIN (RSA |EC |DSA |OPENSSH )?PRIVATE KEY-----"
