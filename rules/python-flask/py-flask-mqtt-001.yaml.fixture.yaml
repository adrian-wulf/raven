rule_id: py-flask-mqtt-001
language: python
examples:
  unsafe: '# Unsafe - py-flask-mqtt-001

    import hashlib

    hashlib.md5(password.encode())

    '
  safe: '# Safe - py-flask-mqtt-001

    # Use parameterized queries, validate inputs, use security libraries

    from django.utils.html import escape

    escape(user_input)

    '
