rule_id: docker-onbuild-001
language: dockerfile
examples:
  unsafe: '# Unsafe - docker-onbuild-001

    FROM ubuntu:latest

    '
  safe: '# Safe - docker-onbuild-001

    # Use pinned versions, non-root user, secrets mount

    FROM ubuntu:22.04

    USER appuser

    '
