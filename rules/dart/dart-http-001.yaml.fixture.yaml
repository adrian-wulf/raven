rule_id: dart-http-001
language: dart
examples:
  unsafe: '// Unsafe - dart-http-001

    var rng = Random();

    '
  safe: '// Safe - dart-http-001

    // Use parameterized APIs, validate inputs

    final stmt = await db.prepare(''SELECT * FROM t WHERE id = ?'');

    '
