id: raven-iac-nginx-003
name: Nginx Missing HSTS
severity: medium
category: misconfiguration
confidence: medium
languages: [nginx]
message: |
  Add Strict-Transport-Security header to enforce HTTPS.
patterns:
  - type: regex
    pattern: "server"
    where:
      - not-sanitized:
          - Strict-Transport-Security
