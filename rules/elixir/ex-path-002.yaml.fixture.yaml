rule_id: ex-path-002
language: elixir
examples:
  unsafe: '# Unsafe - ex-path-002

    File.read!(user_path)

    '
  safe: '# Safe - ex-path-002

    # Use Ecto changesets, parameterized queries

    from u in User, where: u.id == ^user_id

    '
