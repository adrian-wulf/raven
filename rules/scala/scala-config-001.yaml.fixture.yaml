rule_id: scala-config-001
language: scala
examples:
  unsafe: '// Unsafe - scala-config-001

    sql"SELECT * FROM users WHERE id = $userId"

    '
  safe: '// Safe - scala-config-001

    // Use ORM, parameterized queries

    Users.filter(_.id === userId).result

    '
