rule_id: js-angular-directive-001
language: javascript
examples:
  unsafe: '// Unsafe - js-angular-directive-001

    element.innerHTML = userData;

    '
  safe: '// Safe - js-angular-directive-001

    // Use framework features, validate inputs, sanitize data

    const safeHtml = DOMPurify.sanitize(userData);

    '
