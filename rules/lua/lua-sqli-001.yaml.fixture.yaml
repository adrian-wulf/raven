rule_id: lua-sqli-001
language: lua
examples:
  unsafe: '-- Unsafe - lua-sqli-001

    conn:execute("SELECT * FROM t WHERE id=" .. id)

    '
  safe: '-- Safe - lua-sqli-001

    -- Validate inputs, use parameterized APIs

    local stmt = db:prepare(''SELECT * FROM t WHERE id = ?'')

    '
