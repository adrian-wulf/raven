rule_id: scala-slick-001
language: scala
examples:
  unsafe: '// Unsafe - scala-slick-001

    sql"SELECT * FROM users WHERE id = $userId"

    '
  safe: '// Safe - scala-slick-001

    // Use ORM, parameterized queries

    Users.filter(_.id === userId).result

    '
