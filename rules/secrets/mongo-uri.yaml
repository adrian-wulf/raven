id: raven-secrets-mongo-001
name: MongoDB Connection String
severity: high
category: secrets
confidence: high
cwe: CWE-798
languages: [javascript, typescript, python, go, php, java, csharp, rust, ruby, swift, kotlin]
message: |
  MongoDB URI with credentials detected! Use connection string without password in code.
patterns:
  - type: regex
    pattern: "mongodb\\+srv?://[^:]+:[^@]+@"
