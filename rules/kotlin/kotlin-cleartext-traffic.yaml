id: raven-kotlin-cleartext-001
name: Kotlin Android Cleartext Traffic
description: |
  Detects usesCleartextTraffic=true which allows HTTP connections.
severity: high
category: cryptography
confidence: high
cwe: CWE-327
languages: [kotlin]
message: |
  Cleartext traffic sends data unencrypted! Use HTTPS only in production.
patterns:
  - type: regex
    pattern: "android:usesCleartextTraffic=['\"]true['\"]"
