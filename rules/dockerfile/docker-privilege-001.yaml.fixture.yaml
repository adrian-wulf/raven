rule_id: docker-privilege-001
language: dockerfile
examples:
  unsafe: '# Unsafe - docker-privilege-001

    USER root

    '
  safe: '# Safe - docker-privilege-001

    # Use pinned versions, non-root user, secrets mount

    FROM ubuntu:22.04

    USER appuser

    '
