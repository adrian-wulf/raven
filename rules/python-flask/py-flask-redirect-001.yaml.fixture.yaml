rule_id: py-flask-redirect-001
language: python
examples:
  unsafe: '# Unsafe - py-flask-redirect-001

    return redirect(request.GET.get(''next''))

    '
  safe: '# Safe - py-flask-redirect-001

    # Use parameterized queries, validate inputs, use security libraries

    from django.utils.html import escape

    escape(user_input)

    '
