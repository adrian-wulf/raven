rule_id: docker-entrypoint-001
language: dockerfile
examples:
  unsafe: '# Unsafe - docker-entrypoint-001

    # No HEALTHCHECK

    '
  safe: '# Safe - docker-entrypoint-001

    # Use pinned versions, non-root user, secrets mount

    FROM ubuntu:22.04

    USER appuser

    '
