rule_id: dart-cmdi-001
language: dart
examples:
  unsafe: '// Unsafe - dart-cmdi-001

    Process.run("ls $path", []);

    '
  safe: '// Safe - dart-cmdi-001

    // Use parameterized APIs, validate inputs

    final stmt = await db.prepare(''SELECT * FROM t WHERE id = ?'');

    '
