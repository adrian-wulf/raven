id: raven-iac-env-001
name: .env File Hardcoded Secret
severity: high
category: secrets
confidence: high
cwe: CWE-798
languages: [env]
message: |
  Secret committed to .env file! Use secret management or environment injection.
patterns:
  - type: regex
    pattern: "^(API_KEY|SECRET_KEY|AUTH_TOKEN|PW|PASSWORD|PRIVATE_KEY|DATABASE_URL)=.+"
