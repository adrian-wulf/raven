rule_id: docker-privilege-002
language: dockerfile
examples:
  unsafe: '# Unsafe - docker-privilege-002

    USER root

    '
  safe: '# Safe - docker-privilege-002

    # Use pinned versions, non-root user, secrets mount

    FROM ubuntu:22.04

    USER appuser

    '
