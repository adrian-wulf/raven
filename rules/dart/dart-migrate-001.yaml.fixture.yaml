rule_id: dart-migrate-001
language: dart
examples:
  unsafe: '// Unsafe - dart-migrate-001

    http.get(url) // no auth header

    '
  safe: '// Safe - dart-migrate-001

    // Use parameterized APIs, validate inputs

    final stmt = await db.prepare(''SELECT * FROM t WHERE id = ?'');

    '
