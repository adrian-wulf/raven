rule_id: py-django-redirect-001
language: python
examples:
  unsafe: '# Unsafe - py-django-redirect-001

    requests.get(user_url)

    '
  safe: '# Safe - py-django-redirect-001

    # Use parameterized queries, validate inputs, use security libraries

    from django.utils.html import escape

    escape(user_input)

    '
