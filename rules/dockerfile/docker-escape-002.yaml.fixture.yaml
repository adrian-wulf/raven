rule_id: docker-escape-002
language: dockerfile
examples:
  unsafe: '# Unsafe - docker-escape-002

    USER root

    '
  safe: '# Safe - docker-escape-002

    # Use pinned versions, non-root user, secrets mount

    FROM ubuntu:22.04

    USER appuser

    '
