rule_id: lua-ssl-001
language: lua
examples:
  unsafe: '-- Unsafe - lua-ssl-001

    os.execute("ls " .. user_path)

    '
  safe: '-- Safe - lua-ssl-001

    -- Validate inputs, use parameterized APIs

    local stmt = db:prepare(''SELECT * FROM t WHERE id = ?'')

    '
