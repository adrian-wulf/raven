rule_id: bash-ssrf-002
language: bash
examples:
  unsafe: '# Unsafe - bash-ssrf-002

    requests.get(user_url)

    '
  safe: '# Safe - bash-ssrf-002

    # Use parameterized queries, validate inputs, use security libraries

    from django.utils.html import escape

    escape(user_input)

    '
