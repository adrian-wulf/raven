rule_id: docker-workdir-001
language: dockerfile
examples:
  unsafe: '# Unsafe - docker-workdir-001

    USER root

    '
  safe: '# Safe - docker-workdir-001

    # Use pinned versions, non-root user, secrets mount

    FROM ubuntu:22.04

    USER appuser

    '
