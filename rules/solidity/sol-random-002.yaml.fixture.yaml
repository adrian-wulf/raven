rule_id: sol-random-002
language: solidity
examples:
  unsafe: '// Unsafe - sol-random-002

    uint random = uint(blockhash(block.number - 1));

    '
  safe: '// Safe - sol-random-002

    // Use Checks-Effects-Interactions, access controls

    // ReentrancyGuard, SafeMath

    '
