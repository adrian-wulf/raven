rule_id: js-angular-eval-001
language: javascript
examples:
  unsafe: '// Unsafe - js-angular-eval-001

    eval(userCode);

    '
  safe: '// Safe - js-angular-eval-001

    // Use framework features, validate inputs, sanitize data

    const safeHtml = DOMPurify.sanitize(userData);

    '
