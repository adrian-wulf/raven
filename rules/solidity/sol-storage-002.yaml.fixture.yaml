rule_id: sol-storage-002
language: solidity
examples:
  unsafe: '// Unsafe - sol-storage-002

    User storage user; // uninitialized

    '
  safe: '// Safe - sol-storage-002

    // Use Checks-Effects-Interactions, access controls

    // ReentrancyGuard, SafeMath

    '
