id: raven-secrets-npm-001
name: NPM Auth Token
severity: high
category: secrets
confidence: high
languages: [javascript, typescript, python, go, php, java, csharp, rust, ruby, swift, kotlin]
message: |
  NPM auth token detected! Use .npmrc with environment variable references.
patterns:
  - type: regex
    pattern: "npm_[a-zA-Z0-9]{36}"
  - type: regex
    pattern: "_authToken\\s*=\\s*[a-zA-Z0-9]{20,}"
