rule_id: lua-ssrf-002
language: lua
examples:
  unsafe: '-- Unsafe - lua-ssrf-002

    socket.http.request(user_url)

    '
  safe: '-- Safe - lua-ssrf-002

    -- Validate inputs, use parameterized APIs

    local stmt = db:prepare(''SELECT * FROM t WHERE id = ?'')

    '
