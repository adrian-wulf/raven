rule_id: py-fastapi-cors-003
language: python
examples:
  unsafe: '# Unsafe - py-fastapi-cors-003

    CORS(app, origins=''*'')

    '
  safe: '# Safe - py-fastapi-cors-003

    # Use parameterized queries, validate inputs, use security libraries

    from django.utils.html import escape

    escape(user_input)

    '
