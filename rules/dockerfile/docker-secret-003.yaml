id: docker-secret-003
name: ENV API_KEY
severity: critical
category: secrets
confidence: high
cwe: CWE-798
languages:
- dockerfile
message: API_KEY in ENV is visible in docker history. Use Docker secrets instead.
patterns:
- type: regex
  pattern: ENV\s+(API_KEY|api[_-]?key)\s*=\s*\S+
  where: []
references:
- https://cwe.mitre.org/data/definitions/798.html
