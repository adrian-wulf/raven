rule_id: docker-expose-001
language: dockerfile
examples:
  unsafe: '# Unsafe - docker-expose-001

    # No HEALTHCHECK

    '
  safe: '# Safe - docker-expose-001

    # Use pinned versions, non-root user, secrets mount

    FROM ubuntu:22.04

    USER appuser

    '
