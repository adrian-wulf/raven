rule_id: docker-shell-001
language: dockerfile
examples:
  unsafe: '# Unsafe - docker-shell-001

    # No HEALTHCHECK

    '
  safe: '# Safe - docker-shell-001

    # Use pinned versions, non-root user, secrets mount

    FROM ubuntu:22.04

    USER appuser

    '
