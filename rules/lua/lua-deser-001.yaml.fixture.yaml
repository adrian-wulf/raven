rule_id: lua-deser-001
language: lua
examples:
  unsafe: '-- Unsafe - lua-deser-001

    loadstring(user_code)()

    '
  safe: '-- Safe - lua-deser-001

    -- Validate inputs, use parameterized APIs

    local stmt = db:prepare(''SELECT * FROM t WHERE id = ?'')

    '
