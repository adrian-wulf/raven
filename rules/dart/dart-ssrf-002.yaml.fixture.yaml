rule_id: dart-ssrf-002
language: dart
examples:
  unsafe: '// Unsafe - dart-ssrf-002

    http.get(userUrl);

    '
  safe: '// Safe - dart-ssrf-002

    // Use parameterized APIs, validate inputs

    final stmt = await db.prepare(''SELECT * FROM t WHERE id = ?'');

    '
