rule_id: py-fastapi-depends-005
language: python
examples:
  unsafe: "# Unsafe - py-fastapi-depends-005\ndef secret_view(request):\n    return secret_data\n"
  safe: '# Safe - py-fastapi-depends-005

    # Use parameterized queries, validate inputs, use security libraries

    from django.utils.html import escape

    escape(user_input)

    '
