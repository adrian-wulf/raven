rule_id: js-vue-router-001
language: javascript
examples:
  unsafe: '// Unsafe - js-vue-router-001

    router.push(''/admin'');

    '
  safe: '// Safe - js-vue-router-001

    // Use framework features, validate inputs, sanitize data

    const safeHtml = DOMPurify.sanitize(userData);

    '
