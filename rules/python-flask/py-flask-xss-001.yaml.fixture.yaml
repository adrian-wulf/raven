rule_id: py-flask-xss-001
language: python
examples:
  unsafe: '# Unsafe - py-flask-xss-001

    return HttpResponse(f"<div>{user_input}</div>")

    '
  safe: '# Safe - py-flask-xss-001

    # Use parameterized queries, validate inputs, use security libraries

    from django.utils.html import escape

    escape(user_input)

    '
