rule_id: docker-copy-003
language: dockerfile
examples:
  unsafe: '# Unsafe - docker-copy-003

    FROM ubuntu:latest

    '
  safe: '# Safe - docker-copy-003

    # Use pinned versions, non-root user, secrets mount

    FROM ubuntu:22.04

    USER appuser

    '
