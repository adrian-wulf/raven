rule_id: docker-apt-002
language: dockerfile
examples:
  unsafe: '# Unsafe - docker-apt-002

    FROM ubuntu:latest

    '
  safe: '# Safe - docker-apt-002

    # Use pinned versions, non-root user, secrets mount

    FROM ubuntu:22.04

    USER appuser

    '
