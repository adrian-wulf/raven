rule_id: js-nextjs-middleware-001
language: javascript
examples:
  unsafe: '// Unsafe - js-nextjs-middleware-001

    router.push(''/admin'');

    '
  safe: '// Safe - js-nextjs-middleware-001

    // Use framework features, validate inputs, sanitize data

    const safeHtml = DOMPurify.sanitize(userData);

    '
