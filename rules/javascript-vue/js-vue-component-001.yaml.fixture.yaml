rule_id: js-vue-component-001
language: javascript
examples:
  unsafe: '// Unsafe - js-vue-component-001

    eval(userCode);

    '
  safe: '// Safe - js-vue-component-001

    // Use framework features, validate inputs, sanitize data

    const safeHtml = DOMPurify.sanitize(userData);

    '
