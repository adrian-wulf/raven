rule_id: yaml-k8s-017
language: yaml
examples:
  unsafe: '# Unsafe - yaml-k8s-017

    # No resource limits

    '
  safe: "# Safe - yaml-k8s-017\n# Use sealed secrets, security contexts\napiVersion: v1\nkind: Secret\nstringData:\n  password: <from-vault>\n"
