id: raven-secrets-firebase-001
name: Firebase API Key
severity: medium
category: secrets
confidence: medium
languages: [javascript, typescript, python, go, php, java, csharp, rust, ruby, swift, kotlin]
message: |
  Firebase API key detected. While not secret by design, restrict with API key restrictions.
patterns:
  - type: regex
    pattern: "AIza[0-9A-Za-z_-]{35}"
