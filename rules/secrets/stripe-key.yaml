id: raven-secrets-stripe-001
name: Stripe API Key
severity: critical
category: secrets
confidence: high
languages: [javascript, typescript, python, go, php, java, csharp, rust, ruby, swift, kotlin]
message: |
  Stripe key detected! Never commit payment processor keys. Use environment variables.
patterns:
  - type: regex
    pattern: "sk_(live|test)_[0-9a-zA-Z]{24,}"
  - type: regex
    pattern: "pk_(live|test)_[0-9a-zA-Z]{24,}"
