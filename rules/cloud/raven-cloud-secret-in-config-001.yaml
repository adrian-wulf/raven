id: raven-cloud-secret-in-config-001
name: Cloud secret in config file
severity: high
category: secrets
confidence: high
cwe: CWE-798
languages: ["*"]
message: "Cloud API key or secret found in configuration. Use secret manager or vault."
patterns:
  - type: regex
    pattern: "(aws_access_key_id|aws_secret_access_key|AZURE_CLIENT_SECRET|GOOGLE_APPLICATION_CREDENTIALS).*="
