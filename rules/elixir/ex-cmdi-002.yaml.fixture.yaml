rule_id: ex-cmdi-002
language: elixir
examples:
  unsafe: '# Unsafe - ex-cmdi-002

    System.cmd("ls", ["#{path}"])

    '
  safe: '# Safe - ex-cmdi-002

    # Use Ecto changesets, parameterized queries

    from u in User, where: u.id == ^user_id

    '
