id: raven-iac-k8s-008
name: Kubernetes Missing Network Policy
severity: low
category: misconfiguration
confidence: low
cwe: CWE-16
languages: [yaml]
message: |
  No NetworkPolicy detected. Add network policies to restrict pod-to-pod traffic.
patterns:
  - type: regex
    pattern: "kind:\\s*Deployment"
    where:
      - not-sanitized:
          - NetworkPolicy
