rule_id: docker-multi-001
language: dockerfile
examples:
  unsafe: '# Unsafe - docker-multi-001

    FROM ubuntu:latest

    '
  safe: '# Safe - docker-multi-001

    # Use pinned versions, non-root user, secrets mount

    FROM ubuntu:22.04

    USER appuser

    '
