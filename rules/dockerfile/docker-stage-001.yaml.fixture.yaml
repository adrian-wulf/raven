rule_id: docker-stage-001
language: dockerfile
examples:
  unsafe: '# Unsafe - docker-stage-001

    ENV API_KEY=sk-live-abc123

    '
  safe: '# Safe - docker-stage-001

    # Use pinned versions, non-root user, secrets mount

    FROM ubuntu:22.04

    USER appuser

    '
