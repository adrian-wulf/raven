rule_id: py-django-csrf-001
language: python
examples:
  unsafe: "# Unsafe - py-django-csrf-001\n@csrf_exempt\ndef my_view(request):\n    pass\n"
  safe: '# Safe - py-django-csrf-001

    # Use parameterized queries, validate inputs, use security libraries

    from django.utils.html import escape

    escape(user_input)

    '
