rule_id: docker-volume-001
language: dockerfile
examples:
  unsafe: '# Unsafe - docker-volume-001

    # No HEALTHCHECK

    '
  safe: '# Safe - docker-volume-001

    # Use pinned versions, non-root user, secrets mount

    FROM ubuntu:22.04

    USER appuser

    '
