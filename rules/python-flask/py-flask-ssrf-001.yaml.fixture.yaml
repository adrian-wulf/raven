rule_id: py-flask-ssrf-001
language: python
examples:
  unsafe: '# Unsafe - py-flask-ssrf-001

    requests.get(user_url)

    '
  safe: '# Safe - py-flask-ssrf-001

    # Use parameterized queries, validate inputs, use security libraries

    from django.utils.html import escape

    escape(user_input)

    '
