rule_id: ex-genstage-001
language: elixir
examples:
  unsafe: '# Unsafe - ex-genstage-001

    String.duplicate("x", user_count)

    '
  safe: '# Safe - ex-genstage-001

    # Use Ecto changesets, parameterized queries

    from u in User, where: u.id == ^user_id

    '
