id: raven-iac-docker-003
name: Dockerfile Hardcoded Secret
severity: high
category: secrets
confidence: high
languages: [dockerfile]
message: |
  Hardcoded secret in Dockerfile! Use build args or secret mounts.
patterns:
  - type: regex
    pattern: "ENV\\s+(API_KEY|SECRET|TOKEN|PW|PASS|PASSWORD|PRIVATE_KEY)\\s*=\\s*[^\\s]+"
