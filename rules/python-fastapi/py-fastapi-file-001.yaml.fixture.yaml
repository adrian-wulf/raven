rule_id: py-fastapi-file-001
language: python
examples:
  unsafe: '# Unsafe - py-fastapi-file-001

    open(user_path).read()

    '
  safe: '# Safe - py-fastapi-file-001

    # Use parameterized queries, validate inputs, use security libraries

    from django.utils.html import escape

    escape(user_input)

    '
