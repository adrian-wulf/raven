rule_id: py-fastapi-depends-004
language: python
examples:
  unsafe: '# Unsafe - py-fastapi-depends-004

    DEBUG = True

    '
  safe: '# Safe - py-fastapi-depends-004

    # Use parameterized queries, validate inputs, use security libraries

    from django.utils.html import escape

    escape(user_input)

    '
