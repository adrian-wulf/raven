rule_id: ex-cmdi-001
language: elixir
examples:
  unsafe: '# Unsafe - ex-cmdi-001

    System.cmd("ls", ["#{path}"])

    '
  safe: '# Safe - ex-cmdi-001

    # Use Ecto changesets, parameterized queries

    from u in User, where: u.id == ^user_id

    '
