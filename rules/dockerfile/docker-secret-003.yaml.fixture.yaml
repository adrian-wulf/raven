rule_id: docker-secret-003
language: dockerfile
examples:
  unsafe: '# Unsafe - docker-secret-003

    ENV API_KEY=sk-live-abc123

    '
  safe: '# Safe - docker-secret-003

    # Use pinned versions, non-root user, secrets mount

    FROM ubuntu:22.04

    USER appuser

    '
